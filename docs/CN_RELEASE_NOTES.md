中文 fork 独立发行，沿用原工程的 `build.sh`、应用名称 `Recipe Lab` 和包名 `com.voxivoid.recipelab`。

- 应用界面和使用文档中文适配，内置中文字体以解决缺字方块。
- A5100 默认显示配方小标签，MOVIE 键单击循环切换小标签、完整参数和隐藏显示。
- 配方名称行右侧提供原 Fn 配方列表入口，上下键可以在名称、参数与入口之间切换。
- 保留长按中央键收藏，不再用播放键切换显示。

下载附件 `RecipeLab.apk`，通过 Sony-PMCA-RE 安装。该 APK 使用本 fork 的固定签名，不能直接覆盖签名不同的上游官方 APK；卸载官方包会清除其收藏和设置。此前的 `.zh` 开发版是独立包，不会被本包覆盖。

发布前自动运行单元测试、原工程 Release 构建、APK 签名检查及上传后的文件比对。构建成功不代表真机交互验证通过，本发行包仍需在相机上验证。

上游项目：https://github.com/voxivoid/recipe-lab-sony-pmca
